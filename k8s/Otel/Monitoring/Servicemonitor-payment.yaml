apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment-api-monitor
  namespace: default
spec:
  endpoints:
  - interval: 30s
    targetPort: 5180
    path: /metrics
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      target-allocation: "true"